# Orbit Audit Model

Orbit normalizes evidence into Intent, PlanItem, AssistantClaim, ToolEvidence, ValidationResult, Gap, Conflict, and Uncertainty records.

The key comparison is:

```text
intent → plan → claim → tool evidence → repo truth
```
