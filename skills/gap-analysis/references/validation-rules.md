# Validation Rules

Assistant claims must be validated against tool evidence and repo truth. Claims remain unverified until a non-destructive check confirms them.

Default validations: file exists, text exists, symbol exists, artifact exists, executable bit set. Run commands only from explicit safe configuration.
