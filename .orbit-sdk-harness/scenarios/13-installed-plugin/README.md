# Scenario 13 — Installed Plugin Validation

Validate Orbit as a real installed Claude Code plugin where feasible. Check what can be queried non-interactively; mark BLOCKED checks with manual verification commands.
