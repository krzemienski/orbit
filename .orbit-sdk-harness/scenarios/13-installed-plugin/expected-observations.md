# Expected Observations

- `claude` CLI on PATH is detected.
- `~/.claude/plugins/installed_plugins.json` is parsed if present.
- Slash command and skill discovery are reported as BLOCKED with manual verification commands.
