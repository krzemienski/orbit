# Scenario 05 — Dashboard Required

Verify Orbit's static dashboard is rendered with all required sections (summary cards, intent timeline, intent-plan-claim-tool-repo matrix, gaps, claim validation, uncertainty) and the brand palette (lime/orange/red/purple/blue) on a flat black background.
