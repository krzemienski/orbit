# Expected Observations

- The agent reads the parser source.
- The agent reports the actual flag presence.
- The agent names the gap category that would apply on contradiction (`claim-contradicted`).
