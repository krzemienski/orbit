# Expected Observations

- `evidence.json` scope echoes the selected window for each invocation.
- Markdown report and dashboard state the selected window.
- `review-last-3-days` is documented as alias for `--days 3`.
