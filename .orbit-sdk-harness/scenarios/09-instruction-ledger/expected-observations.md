# Expected Observations

- The agent enumerates ≥6 signal phrases.
- The agent enumerates the JSONL record fields (`id`, `timestamp`, `kind`, `text`, `source_ref`, `status`, etc.).
- An actual `intent-ledger.jsonl` is built from the bundled fixture and verified line-by-line.
