# Documentation Research Index

| File | Purpose |
|---|---|
| `SUMMARY.md` | Five sources × ≥3 cited facts each: `claude-agent-sdk`, plugin manifest contract, hook I/O contract, skill frontmatter, slash command format |

All sources are local (installed package introspection + on-disk Orbit files). No remote fetches.
