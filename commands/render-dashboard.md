# Render Dashboard

Render Orbit's static dashboard from an evidence JSON file.

```bash
orbit-audit render-dashboard --evidence .claude/audits/latest/evidence.json $ARGUMENTS
```

`$ARGUMENTS` may override `--evidence <path>` or `--output <path>`.
